"use client";

import { links } from "@/data/links";
import Image from "next/image";
import { motion } from "motion/react";

const Hero = () => {
  return (
    <div className="flex flex-col flex-1 items-center justify-center font-sans">
      <main className="flex flex-1 w-full max-w-3xl min-h-screen flex-col items-center justify-between px-6 py-8 sm:items-start"></main>
    </div>
  );
};

export default Hero;
